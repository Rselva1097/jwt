#spring:
#  application:
#    name: Jwt
#
#  datasource:
#   url: jdbc:postgresql://localhost:5432/jwt_token
#   username: postgres
#   password: admin
#   driver-class-name: org.postgresql.Driver
#   jpa:
#    hibernate:
#     ddl-auto: update
#     database: postgresql
#      database-platform: org.hibernate.dialect.PostgreSQLDialect
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_token
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


application:
  security:
    jwt:
      secret-key:  Lcpk96lrJnlbXGtFmNLmm2uzbLgPk2oqaaX/REtvCoT6mGBKucboaAi7yhtkqEK3
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days
